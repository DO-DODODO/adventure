# Adventure - 화면 흐름 & 인터랙션 설계

RULES.md(게임 규칙), AI_STRATEGY.md(AI 판단 기준)를 전제로, 실제 화면 동작을 정리한다.

## 화면 구성

1. **메인 화면** (`img/main-menu.png` 그대로 사용)
   - 싱글모드 / 멀티모드 / 게임룰 버튼 3개가 이미지에 그려져 있음
   - 이번 구현 범위: **싱글모드만 클릭 가능**, 나머지 두 개는 비활성(클릭 무반응)
2. **로딩 연출**
   - 싱글모드 클릭 시, 화면 중앙에 레트로 모래시계 아이콘이 잠깐 떴다가 사라짐
   - 그 뒤로는 이미 카드 분배까지 끝난 게임 화면이 깔려있음 (모래시계가 사라지면 바로 플레이 가능한 상태)
3. **게임 화면** (기존 시안 레이아웃 그대로: 상대 손패 → 상대 탐험로 → 보드+드로우더미 → 내 탐험로 → 내 손패)
4. **점수 화면**
   - 색깔별 점수 5줄 + 총점
   - 버튼 2개: "한 판 더!" / "게임종료"

## 게임 시작 시퀀스

1. 60장 셔플
2. 나 8장, 상대(AI) 8장 분배 (드로우 더미 = 44장)
3. 보드 5칸 전부 비어있는 상태로 시작
4. 선공 결정
   - **세션 첫 판**: 랜덤으로 결정
   - **다음 판부터**: 직전 판 승자가 선공 (점수 더 높은 쪽. 동점이면 랜덤)
5. 상대 캐릭터: 세션 시작 시 랜덤으로 하나 배정, **세션이 끝날 때까지 고정** (판이 바뀌어도 안 바뀜)

## 내 턴 - 인터랙션

1. **카드 선택**: 손패 카드 탭
   - 그 카드가 살짝 위로 뜸(선택 상태 표시)
   - 놓을 수 있는 자리 하이라이트:
     - 같은 색 버림더미 칸(보드) — 항상 하이라이트 가능
     - 같은 색 내 탐험로 자리 — **규칙상 놓을 수 있을 때만** 하이라이트 (예: 이미 6이 놓여있는데 4를 냈으면 오름차순 위반이라 탐험로 쪽은 하이라이트 안 함, 버림더미만 하이라이트)
   - **선택 취소**: 뜬 카드를 다시 탭하면 하이라이트 전부 해제 + 카드 제자리로 복귀
2. **카드 배치**: 하이라이트된 자리 중 하나 탭 → 그 자리에 카드 놓임
3. **카드 획득 (필수)**: 배치 직후, 가져올 수 있는 곳 전부 하이라이트
   - 드로우 더미
   - 버림더미 중 카드가 있는 칸 전부 (빈 칸은 하이라이트 안 함)
   - 하나 탭하면 그 카드를 손으로 가져오고 턴 종료

## 상대(AI) 턴

- 내 턴이 끝나면 상대 아바타에 "지금 이 사람 턴" 하이라이트 표시
- AI도 사람과 동일한 절차로 진행 (연출 포함):
  1. AI_STRATEGY.md 기준으로 낼 카드/자리 결정 → 그 카드가 위로 뜸 → 해당 자리로 이동해 배치
  2. 가져올 곳 결정(드로우 더미 vs 버림더미) → 그쪽 하이라이트 → 가져옴
  3. 턴 종료, 다시 내 턴으로

## 게임 종료 & 점수

- 드로우 더미가 바닥나면(마지막 카드 가져간 사람 턴까지 진행 후) 종료
- 색깔별 점수 계산(RULES.md 공식) → 점수 화면 표시
- "한 판 더!" → 위 "게임 시작 시퀀스"부터 다시 (선공은 이번 판 승자, 상대 캐릭터는 유지)
- "게임종료" → 메인 화면으로 복귀
